{% macro input( name, value, type="text", size="20", placeholder="value", class ) %}
    <input  type="{{ type }}" 
            name="{{ name }}" 
            value="{{ value }}" 
            size="{{ size }}" 
            placeholder={{ placeholder }}
            class="{{ class }}"
    />
{% endmacro %}